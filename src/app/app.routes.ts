import { Routes } from '@angular/router';
import { EsgOverrideComponent } from './esg-override/esg-override.component';

export const routes: Routes = [
  { path: '', component: EsgOverrideComponent }
];